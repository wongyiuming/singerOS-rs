[package]
name = "audio-worklet-lab"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[target.'cfg(target_arch = "wasm32")'.dependencies]
cpal = { version = "0.18.2", default-features = false, features = ["audioworklet"] }
